exports.config = { 
    serverPort: 3005, // if you change the port here, make sure to do the same on "internet-banking-front\src\services\api.js"
    databaseName: 'internet-banking', //database's schema name
    databaseUser: 'root', //database's user
    databasePassword: 'admin', //database's password
    databaseHost: 'localhost', //database's host
    databasePort: 3300 //database's port
};
